{
  "name": "attendance-management-server",
  "version": "1.0.0",
  "description": "weTech Attendance Management - API server",
  "main": "index.js",
  "private": true,
  "engines": { "node": ">=18.0.0" },
  "comment": "Dependencies live in the repository root package.json so one `npm install` covers the API, the tests and the Vercel build. Node resolves them from here by walking up.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "seed": "node scripts/seed.js",
    "test": "node --test tests/domain.test.js tests/policy.test.js tests/api.test.js tests/rules.test.js tests/serverless.test.js tests/config.test.js",
    "test:e2e": "node --test --test-timeout=180000 tests/browser.test.js",
    "icons": "node ../tools/make-icons.js"
  }
}
